# Optional: visitor counter (Upstash Redis). Without these, the counter is hidden.
# Create a free database at https://upstash.com and copy REST URL + token.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
